# Copy this file to .env and fill in your values
# This is the URL prefix where the backend is served (Apache/XAMPP on port 80).
# It must exactly match the folder name inside Apache's web root (htdocs/).
VITE_API_BASE_URL=/stayvora-backend